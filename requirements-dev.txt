-r requirements.txt

# Testing - only needed to run automated checks while developing; not required to run the API.
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# Linting/type checks - catches style issues and common bugs before running or committing code.
ruff>=0.1.0
mypy>=1.8.0
pre-commit>=3.6.0

# Dev helpers - reload/file-watch and extra HTTP test utilities for local development.
watchfiles>=0.21.0
httpx
