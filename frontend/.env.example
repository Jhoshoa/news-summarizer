# API base URL: empty = same origin (works when nginx proxies /api to backend)
# Set to full URL if backend is on a different domain:
# VITE_API_BASE_URL=https://api.tudominio.com
VITE_API_BASE_URL=

VITE_ENABLE_MANUAL_REFRESH=false
